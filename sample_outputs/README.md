# Sample outputs

This directory contains small submission-safe outputs generated by the measured Gate D project-artifact run.

Expected evidence:
- `ar_classification.json` — Arabic FastAPI classification response.
- `en_classification.json` — English FastAPI classification response.
- `invalid_input.json` — verified invalid-input response.

The files are generated from synthetic course examples and must not contain personal data, secrets, model weights, or large artefacts.
